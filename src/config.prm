Parameters 
{
	omega           1.8;
	initialVelocity < 0, 0, 0>;
	// REMARK: I removed the initial velocity, as it seems you can not use a vector
	// to initialize the velocity field (only a scalar value)
	timesteps       5000;
	remainingTimeLoggerFrequency 3.0; // in seconds
    VTKwriteFrequency 50;
}

DomainSetup
{
   meshFile         AN182_colored_faces.obj;
   dx               0.1;
   cellsPerBlock    < 16, 16, 16 >;
}

StabilityChecker
{
   checkFrequency 100;
}